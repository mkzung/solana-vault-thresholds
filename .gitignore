.anchor
.DS_Store
target
node_modules
test-ledger
.yarn

*.rs.bk
*.so
.idea/
.vscode/

# Cargo.lock IS committed (this program is a deployable binary, not a library).
# Listed here for clarity; not actually ignored.
!Cargo.lock
